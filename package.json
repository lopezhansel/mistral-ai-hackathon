{
  "name": "mistral-ai-hackathon",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "start": "npm run start --workspace=backend",
    "dev": "npm run dev --workspace=backend",
    "build": "npm run build --workspaces--workspaces --if-present",
    "lint": "npm run lint --workspaces--workspaces --if-present",
    "check": "npm run check --workspaces --if-present"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "frontend",
    "backend",
    "db",
    "trpc"
  ]
}
